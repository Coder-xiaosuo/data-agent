# 复制为 .env 后填入真实值。.env 已被 .gitignore 忽略，不要提交。
#
# 环境变量命名约定：<PROVIDER>_API_KEY，与 llm/registry.py 的 create_client_from_env 对应。
# provider 名取自 llm/registry.py 的 PROVIDERS。

DEEPSEEK_API_KEY=
OPENAI_API_KEY=
